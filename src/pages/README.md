bkuicli-vue3

# 开发环境

## 默认环境（不指定env参数）
npm run dev              # 使用 .bk.local.default.env 作为默认环境

## 自定义环境（可选）
npm run dev --env=dev    # 使用 .bk.local.dev.env
npm run dev --env=test   # 使用 .bk.local.test.env
